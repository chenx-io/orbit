[package]
name = "orbit-plugin-api"
version.workspace = true
license = "MIT OR Apache-2.0"
edition = "2021"
repository = "https://github.com/chenx-io/orbit"
description = "orbit-plugin-api for Orbit "

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
